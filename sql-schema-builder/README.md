# SQL Schema Builder

Визуальный редактор схем баз данных с генерацией SQL для Microsoft Access и других СУБД.

## Возможности

- 🎨 **Визуальный редактор** — drag-and-drop интерфейс для создания таблиц
- 🔗 **Связи между таблицами** — создание связей с визуальными линиями и подписями кардинальности
- ⚙️ **Настройка связей** — выбор мощности (1:1, 1:N, N:1, N:M) и обязательности полей
- 📝 **Генерация SQL** — автоматическая генерация CREATE TABLE и ALTER TABLE с FOREIGN KEY
- 💾 **Экспорт/Импорт** — сохранение и загрузка схем в JSON
- 🌙 **Тёмная/Светлая тема** — переключение тем оформления
- 📊 **Предпросмотр SQL** — ре-time генерация SQL кода
- 🔧 **Поддержка типов данных Access** — TEXT, INTEGER, LONG, DOUBLE, CURRENCY, DATETIME, BOOLEAN, AUTOINCREMENT, MEMO, OLE

## Поддерживаемые СУБД

- Microsoft Access (Jet SQL)
- SQLite
- MySQL
- PostgreSQL

## Установка

### Требования

- Node.js 18+ и npm

### Шаги установки

1. Установите зависимости:
```bash
npm install
```

2. Запустите приложение в режиме разработки:
```bash
npm run dev
```

3. Откройте браузер по адресу `http://localhost:3000`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist`.

## Использование

### Создание таблицы

1. Нажмите кнопку "+ Таблица" на панели инструментов
2. Или перетащите элемент "Таблица" из боковой панели на холст
3. Заполните название таблицы и добавьте колонки

### Редактирование таблицы

- Дважды кликните на таблицу для редактирования
- Или нажмите кнопку "✎" в заголовке таблицы

### Создание связей

1. Нажмите на точку справа от колонки (источник)
2. Перетащите к точке слева от колонки в другой таблице (цель)
3. Связь будет создана автоматически

### Настройка связей

- Кликните на линию связи для открытия настроек
- **Мощность связи**: 1:1, 1:N, N:1, N:M
- **Обязательность**: укажите, может ли поле быть пустым (0..1 или 1)
- **ON DELETE**: CASCADE, SET NULL, RESTRICT, NO ACTION
- **ON UPDATE**: CASCADE, SET NULL, RESTRICT, NO ACTION
- Подписи на линиях отображают текущую мощность (например: "1:N", "0..1:N")

### Настройка колонки

- **PK** — первичный ключ (Primary Key)
- **NOT NULL** — обязательное поле
- **UNIQUE** — уникальное значение
- **Тип данных** — выберите из списка поддерживаемых типов

### Экспорт SQL

1. Выберите диалект SQL в выпадающем меню
2. Нажмите "Экспорт" → "Экспорт SQL"
3. Файл `schema.sql` будет скачан

### Сохранение/Загрузка

- Схема автоматически сохраняется в localStorage браузера
- Экспортируйте в JSON для резервного копирования
- Импортируйте JSON для восстановления схемы

## Типы данных для Access

| Тип | Описание |
|-----|----------|
| TEXT | Текст до 255 символов |
| INTEGER | Целое число |
| LONG | Длинное целое |
| DOUBLE | Число с плавающей точкой |
| CURRENCY | Денежный формат |
| DATETIME | Дата и время |
| BOOLEAN | Логическое значение (Да/Нет) |
| AUTOINCREMENT | Автоинкремент (COUNTER) |
| MEMO | Длинный текст |
| OLE | OLE объект |

## Пример сгенерированного SQL

```sql
CREATE TABLE [Users] (
    [id] COUNTER NOT NULL,
    [name] VARCHAR(255) NOT NULL,
    [email] VARCHAR(255) UNIQUE NOT NULL,
    CONSTRAINT [PK_Users] PRIMARY KEY (id)
);

CREATE TABLE [Orders] (
    [id] COUNTER NOT NULL,
    [user_id] INTEGER NOT NULL,
    [order_date] DATETIME NOT NULL,
    CONSTRAINT [PK_Orders] PRIMARY KEY (id)
);

ALTER TABLE [Orders]
    ADD CONSTRAINT [FK_Orders_Users] FOREIGN KEY ([user_id])
    REFERENCES [Users]([id])
    ON DELETE CASCADE
    ON UPDATE CASCADE;
```

## Технологии

- React 18 + TypeScript
- React Flow для визуализации
- Bootstrap 5 для стилей
- Vite для сборки

## Лицензия

MIT
